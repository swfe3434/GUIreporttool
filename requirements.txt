instagrapi==1.20.1  # Or the latest compatible version
cryptography==42.0.5 # Or the latest compatible version
rich==13.7.1 # Or the latest compatible version
python-dotenv==1.0.1 # Or the latest compatible version
